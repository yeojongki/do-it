# mvvm

MVVM 框架 vue/react 的简单实现

依赖 parcel https://parceljs.org/

> 首先使用 Yarn 或 npm 安装 Parcel ：

Yarn:

```js
yarn global add parcel-bundler
```

npm:

```
npm install -g parcel-bundler
```

> 安装完成后：

```js
yarn dev:vue // 运行开发环境vue版本
yarn build:vue // 运行生产环境vue版本
```
